[pytest]
testpaths = consultant-management/tests employee-management/tests vendor-management/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts = -v --tb=short

markers =
    tc_id(id): Excel test case ID reference
    positive: Positive creation test cases
    negative: Negative validation test cases
    smoke: Core sanity smoke tests
    regression: Full regression test suite
    login: Login and authentication verification tests
    security: Security and authorization tests
    employee: Employee management test cases
    vendor: Vendor management test cases

log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)s] %(name)s: %(message)s
log_cli_date_format = %H:%M:%S
